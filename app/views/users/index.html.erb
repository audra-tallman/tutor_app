<h1>Current Users</h1>
  <ul>
  <% @users.each do |user| %>
    <li><%= link_to "#{user.name}" , user_path(user) %>
      <% if user.admin %>
       - ADMIN
      <% end %>
    </li>
  <% end %>
  </ul>
