<h1> Subjects for Tutoring </h1>
<p> Select a subject that your student needs help with to see what tutors are available. </p>
<ul>
  <% @tutors.all.distinct.pluck(:subject).each do |subject| %>
    <li> <%= "#{subject}" %> </li>
  <% end %>
</ul>

<%= link_to "View Tutors", tutors_path %>
